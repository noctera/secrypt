#ifndef CONFIG_H
#define CONFIG_H

#include "imgui.h"

namespace secrypt {
  namespace ui {
    namespace config {
      ImGuiWindowFlags getWindowFlags();
    }  // namespace config
  }    // namespace ui
}  // namespace secrypt

#endif